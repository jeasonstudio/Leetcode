/**
 * @param  string   line 单行测试数据
 * @return string        返回处理结果
 */
function solution(line) {
  // 在此处理单行数据
  const [a, b] = line.split(',')
  const kongweizi = Number(a) + 1
  const banzi = Number(b) - 1

  // 返回处理后的结果
  return
}

console.log(solution('7,3'))
